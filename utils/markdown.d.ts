/** Serialize the editor's HTML subset to Markdown. */
export declare function toMarkdown(html: string): string;
/** Parse the supported Markdown subset into the editor's HTML. */
export declare function fromMarkdown(markdown: string): string;
